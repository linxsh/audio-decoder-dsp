CROSS_COMPILER_PATH= 
CC      = $(CROSS_COMPILER_PATH)gcc
CPP     = $(CROSS_COMPILER_PATH)g++
LD      = $(CROSS_COMPILER_PATH)ld
AS      = $(CROSS_COMPILER_PATH)as
AR      = $(CROSS_COMPILER_PATH)ar
OBJCOPY = $(CROSS_COMPILER_PATH)objcopy
OBJDUMP = $(CROSS_COMPILER_PATH)objdump
NM      = $(CROSS_COMPILER_PATH)nm
STRIP   = $(CROSS_COMPILER_PATH)strip
READELF = $(CROSS_COMPILER_PATH)readelf -a

PLATFORM = x86
TARGETS  = x86.elf
